#include<stdio.h>
int main()
{
    int n=0,a[n],i,l,h,m,e;
    while(l<=h)
        m=(l+h)/2;
        if(a[m]==e)
            printf("The element is found ")
            l=a[m]-1;
        else(a[m]<e)
            h=a[m]+1;
        
        


}